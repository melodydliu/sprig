# Forage environment — not needed for the mock-data phase (Milestones 1-4).
# Fill these in at Milestone 5 when Supabase is wired up.

# Supabase project URL + anon (publishable) key
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Android only: Google Maps SDK key (Milestone: Android). Not used on iOS (Apple Maps).
# GOOGLE_MAPS_ANDROID_API_KEY=
