/** Seed data moved to the backend-neutral `src/data/seed.ts`; re-exported here
 *  so the mock layer's imports keep working unchanged. */
export { MOCK_USER_ID, buildSeedEntries } from '@/data/seed';
